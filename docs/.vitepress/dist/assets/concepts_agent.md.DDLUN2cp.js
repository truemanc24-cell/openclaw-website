import{_ as e,o as a,c as o,a2 as t}from"./chunks/framework.CGYl2SZ_.js";const l=JSON.parse('{"title":"agent","description":"agent 页面","frontmatter":{"title":"agent","description":"agent 页面"},"headers":[],"relativePath":"concepts/agent.md","filePath":"concepts/agent.md"}');const d=e({name:"concepts/agent.md"},[["render",function(e,l,d,s,i,c){return a(),o("div",null,[...l[0]||(l[0]=[t('<h1 id="代理运行时" tabindex="-1">代理运行时 <a class="header-anchor" href="#代理运行时" aria-label="Permalink to &quot;代理运行时&quot;">​</a></h1><h1 id="代理运行时-🤖" tabindex="-1">代理运行时 🤖 <a class="header-anchor" href="#代理运行时-🤖" aria-label="Permalink to &quot;代理运行时 🤖&quot;">​</a></h1><p>OpenClaw 运行一个基于 <strong>pi-mono</strong> 的单一嵌入式代理运行时。</p><h2 id="工作空间-必需" tabindex="-1">工作空间（必需） <a class="header-anchor" href="#工作空间-必需" aria-label="Permalink to &quot;工作空间（必需）&quot;">​</a></h2><p>OpenClaw 使用单个代理工作空间目录（<code>agents.defaults.workspace</code>）作为代理的<strong>唯一</strong>工作目录（<code>cwd</code>），用于工具和上下文。</p><p>建议：使用 <code>openclaw setup</code> 创建 <code>~/.openclaw/openclaw.json</code>（如果缺失）并初始化工作空间文件。</p><p>完整工作空间布局 + 备份指南：<a href="/concepts/agent-workspace.html">代理工作空间</a></p><p>如果启用了 <code>agents.defaults.sandbox</code>，非主会话可以使用 <code>agents.defaults.sandbox.workspaceRoot</code> 覆盖为每个会话的工作空间（参见<a href="/gateway/configuration.html">网关配置</a>）。</p><h2 id="引导文件-注入" tabindex="-1">引导文件（注入） <a class="header-anchor" href="#引导文件-注入" aria-label="Permalink to &quot;引导文件（注入）&quot;">​</a></h2><p>在 <code>agents.defaults.workspace</code> 中，OpenClaw 期望以下用户可编辑文件：</p><ul><li><code>AGENTS.md</code> — 操作说明 + &quot;记忆&quot;</li><li><code>SOUL.md</code> — 人设、边界、语气</li><li><code>TOOLS.md</code> — 用户维护的工具笔记（如 <code>imsg</code>、<code>sag</code>、约定）</li><li><code>BOOTSTRAP.md</code> — 一次性首次运行仪式（完成后删除）</li><li><code>IDENTITY.md</code> — 代理名称/风格/表情</li><li><code>USER.md</code> — 用户资料 + 首选称呼</li></ul><p>在新会话的第一轮，OpenClaw 会将这些文件的内容直接注入到代理上下文中。</p><p>空白文件会被跳过。大文件会被修剪和截断，并添加标记以保持提示简洁（读取文件以获取完整内容）。</p><p>如果文件缺失，OpenClaw 会注入一行单一的&quot;缺失文件&quot;标记（<code>openclaw setup</code> 将创建安全默认模板）。</p><p><code>BOOTSTRAP.md</code> 仅在<strong>全新工作空间</strong>（无其他引导文件）时创建。如果在完成仪式后删除它，后续重启时不应重新创建。</p><p>要完全禁用引导文件创建（用于预填充的工作空间），请设置：</p><div class="language-json5 vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">skipBootstrap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><h2 id="内置工具" tabindex="-1">内置工具 <a class="header-anchor" href="#内置工具" aria-label="Permalink to &quot;内置工具&quot;">​</a></h2><p>核心工具（read/exec/edit/write 及相关系统工具）始终可用，但需遵守工具策略。<code>apply_patch</code> 是可选的，由 <code>tools.exec.applyPatch</code> 控制。<code>TOOLS.md</code> <strong>不</strong>控制存在哪些工具；它只是关于<em>你</em>希望如何使用它们的指导。</p><h2 id="技能" tabindex="-1">技能 <a class="header-anchor" href="#技能" aria-label="Permalink to &quot;技能&quot;">​</a></h2><p>OpenClaw 从三个位置加载技能（名称冲突时工作空间优先）：</p><ul><li>捆绑（随安装一起分发）</li><li>托管/本地：<code>~/.openclaw/skills</code></li><li>工作空间：<code>&lt;workspace&gt;/skills</code></li></ul><p>技能可以通过配置/环境限制（参见<a href="/gateway/configuration.html">网关配置</a>中的 <code>skills</code>）。</p><h2 id="pi-mono-集成" tabindex="-1">pi-mono 集成 <a class="header-anchor" href="#pi-mono-集成" aria-label="Permalink to &quot;pi-mono 集成&quot;">​</a></h2><p>OpenClaw 重用了 pi-mono 代码库的片段（模型/工具），但<strong>会话管理、发现和工具接线由 OpenClaw 所有</strong>。</p><ul><li>无 pi-coding 代理运行时。</li><li>不读取 <code>~/.pi/agent</code> 或 <code>&lt;workspace&gt;/.pi</code> 设置。</li></ul><h2 id="会话" tabindex="-1">会话 <a class="header-anchor" href="#会话" aria-label="Permalink to &quot;会话&quot;">​</a></h2><p>会话记录以 JSONL 格式存储在：</p><ul><li><code>~/.openclaw/agents/&lt;agentId&gt;/sessions/&lt;SessionId&gt;.jsonl</code></li></ul><p>会话 ID 是稳定的，由 OpenClaw 选择。 旧的 Pi/Tau 会话文件夹<strong>不会被读取</strong>。</p><h2 id="流式传输时转向" tabindex="-1">流式传输时转向 <a class="header-anchor" href="#流式传输时转向" aria-label="Permalink to &quot;流式传输时转向&quot;">​</a></h2><p>当队列模式为 <code>steer</code> 时，入站消息会被注入到当前运行中。队列在<strong>每个工具调用后</strong>检查；如果存在排队的消息，当前助手消息中的剩余工具调用会被跳过（带有&quot;Skipped due to queued user message.&quot;的错误工具结果），然后在下一个助手响应之前注入排队的用户消息。</p><p>当队列模式为 <code>followup</code> 或 <code>collect</code> 时，入站消息会被保留直到当前轮次结束，然后新的代理轮次以排队的负载开始。参见<a href="/concepts/queue.html">队列</a>了解模式 + 防抖/上限行为。</p><p>块流式传输会在完成的助手块完成后立即发送；它<strong>默认关闭</strong>（<code>agents.defaults.blockStreamingDefault: &quot;off&quot;</code>）。 通过 <code>agents.defaults.blockStreamingBreak</code>（<code>text_end</code> vs <code>message_end</code>；默认为 text_end）调整边界。 使用 <code>agents.defaults.blockStreamingChunk</code> 控制软块分块（默认为 800-1200 字符；优先段落换行，然后是换行；最后是句子）。 使用 <code>agents.defaults.blockStreamingCoalesce</code> 合并流式块以减少单行垃圾（发送前基于空闲的合并）。非 Telegram 渠道需要显式 <code>*.blockStreaming: true</code> 才能启用块回复。 详细工具摘要在工具开始时发出（无防抖）；Control UI 在可用时通过代理事件流式传输工具输出。 更多详情：<a href="/concepts/streaming.html">流式传输 + 分块</a>。</p><h2 id="模型引用" tabindex="-1">模型引用 <a class="header-anchor" href="#模型引用" aria-label="Permalink to &quot;模型引用&quot;">​</a></h2><p>配置中的模型引用（例如 <code>agents.defaults.model</code> 和 <code>agents.defaults.models</code>）通过在<strong>第一个</strong><code>/</code> 处分割来解析。</p><ul><li>配置模型时使用 <code>provider/model</code>。</li><li>如果模型 ID 本身包含 <code>/</code>（OpenRouter 风格），请包含提供商前缀（例如 <code>openrouter/moonshotai/kimi-k2</code>）。</li><li>如果省略提供商，OpenClaw 将输入视为别名或<strong>默认提供商</strong>的模型（仅在模型 ID 中没有 <code>/</code> 时有效）。</li></ul><h2 id="配置-最小" tabindex="-1">配置（最小） <a class="header-anchor" href="#配置-最小" aria-label="Permalink to &quot;配置（最小）&quot;">​</a></h2><p>至少设置：</p><ul><li><code>agents.defaults.workspace</code></li><li><code>channels.whatsapp.allowFrom</code>（强烈推荐）</li></ul><hr><p><em>下一节：<a href="/channels/group-messages.html">群组消息</a></em> 🦞</p>',42)])])}]]);export{l as __pageData,d as default};
