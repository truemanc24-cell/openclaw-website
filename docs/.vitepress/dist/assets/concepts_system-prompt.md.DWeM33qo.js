import{_ as o,o as e,c as t,a2 as a}from"./chunks/framework.CGYl2SZ_.js";const s=JSON.parse('{"title":"system prompt","description":"system prompt 页面","frontmatter":{"title":"system prompt","description":"system prompt 页面"},"headers":[],"relativePath":"concepts/system-prompt.md","filePath":"concepts/system-prompt.md"}');const n=o({name:"concepts/system-prompt.md"},[["render",function(o,s,n,l,r,d){return e(),t("div",null,[...s[0]||(s[0]=[a('<h1 id="系统提示" tabindex="-1">系统提示 <a class="header-anchor" href="#系统提示" aria-label="Permalink to &quot;系统提示&quot;">​</a></h1><h1 id="系统提示-1" tabindex="-1">系统提示 <a class="header-anchor" href="#系统提示-1" aria-label="Permalink to &quot;系统提示&quot;">​</a></h1><p>OpenClaw 为每个代理运行构建自定义系统提示。提示<strong>由 OpenClaw 所有</strong>，不使用 pi-coding-agent 默认提示。</p><p>提示由 OpenClaw 组装并注入每个代理运行。</p><h2 id="结构" tabindex="-1">结构 <a class="header-anchor" href="#结构" aria-label="Permalink to &quot;结构&quot;">​</a></h2><p>提示有意紧凑并使用固定部分：</p><ul><li><strong>工具</strong>：当前工具列表 + 简短描述。</li><li><strong>安全</strong>：简短的护栏提醒，避免权力寻求行为或绕过监督。</li><li><strong>技能</strong>（当可用）：告诉模型如何按需加载技能说明。</li><li><strong>OpenClaw 自我更新</strong>：如何运行 <code>config.apply</code> 和 <code>update.run</code>。</li><li><strong>工作空间</strong>：工作目录（<code>agents.defaults.workspace</code>）。</li><li><strong>文档</strong>：OpenClaw 文档的本地路径（repo 或 npm 包）以及何时读取它们。</li><li><strong>工作空间文件（注入）</strong>：表示引导文件包含在下方。</li><li><strong>沙盒</strong>（启用时）：表示沙盒运行时、沙盒路径，以及是否可用提升执行。</li><li><strong>当前日期和时间</strong>：用户本地时间、时区和时间格式。</li><li><strong>回复标签</strong>：支持提供商的可选回复标签语法。</li><li><strong>心跳</strong>：心跳提示和确认行为。</li><li><strong>运行时</strong>：主机、操作系统、节点、模型、repo 根（检测到时）、推理级别（一行）。</li><li><strong>推理</strong>：当前可见性级别 + /reasoning 切换提示。</li></ul><p>系统提示中的安全护栏是指导性的。它们指导模型行为，但不强制执行策略。使用工具策略、执行批准、沙盒和渠道白名单进行强制执行；运营商可以按设计禁用这些。</p><h2 id="提示模式" tabindex="-1">提示模式 <a class="header-anchor" href="#提示模式" aria-label="Permalink to &quot;提示模式&quot;">​</a></h2><p>OpenClaw 可以为子代理渲染较小的系统提示。运行时为每次运行设置一个 <code>promptMode</code>（不是用户面对的配置）：</p><ul><li><code>full</code>（默认）：包含上述所有部分。</li><li><code>minimal</code>：用于子代理；省略<strong>技能</strong>、<strong>记忆召回</strong>、<strong>OpenClaw 自我更新</strong>、<strong>模型别名</strong>、<strong>用户身份</strong>、<strong>回复标签</strong>、<strong>消息</strong>、<strong>静默回复</strong>和<strong>心跳</strong>。工具、<strong>安全</strong>、工作空间、沙盒、当前日期和时间（已知时）、运行时和注入上下文保持可用。</li><li><code>none</code>：仅返回基本身份行。</li></ul><p>当 <code>promptMode=minimal</code> 时，额外注入的提示标记为<strong>子代理上下文</strong>而不是<strong>群组聊天上下文</strong>。</p><h2 id="工作空间引导注入" tabindex="-1">工作空间引导注入 <a class="header-anchor" href="#工作空间引导注入" aria-label="Permalink to &quot;工作空间引导注入&quot;">​</a></h2><p>引导文件被修剪并附加在<strong>项目上下文</strong>下，以便模型看到身份和配置文件上下文，而无需显式读取：</p><ul><li><code>AGENTS.md</code></li><li><code>SOUL.md</code></li><li><code>TOOLS.md</code></li><li><code>IDENTITY.md</code></li><li><code>USER.md</code></li><li><code>HEARTBEAT.md</code></li><li><code>BOOTSTRAP.md</code>（仅在全新工作空间）</li><li><code>MEMORY.md</code>（如果存在），否则 <code>memory.md</code> 作为小写回退</li></ul><p>所有这些文件在每轮都<strong>注入到上下文窗口中</strong>，这意味着它们消耗令牌。保持简洁——特别是 <code>MEMORY.md</code>，它会随着时间增长并导致意外的更高上下文使用和更频繁的压缩。</p><blockquote><p><strong>注意：</strong> <code>memory/*.md</code> 每日文件<strong>不会</strong>自动注入。它们通过 <code>memory_search</code> 和 <code>memory_get</code> 工具按需访问，因此除非模型明确读取它们，否则不会计入上下文窗口。</p></blockquote><p>大文件会被截断并带有标记。最大每文件大小由 <code>agents.defaults.bootstrapMaxChars</code>（默认：20000）控制。文件间注入的引导内容总量由 <code>agents.defaults.bootstrapTotalMaxChars</code>（默认：150000）限制。缺失文件注入短缺失文件标记。发生截断时，OpenClaw 可以在项目上下文中注入警告块；使用 <code>agents.defaults.bootstrapPromptTruncationWarning</code>（<code>off</code>、<code>once</code>、<code>always</code>；默认：<code>once</code>）控制此行为。</p><p>子代理会话仅注入 <code>AGENTS.md</code> 和 <code>TOOLS.md</code>（其他引导文件被过滤掉以保持子代理上下文小）。</p><p>内部钩子可以通过 <code>agent:bootstrap</code> 拦截此步骤以改变或替换注入的引导文件（例如交换 <code>SOUL.md</code> 为替代人设）。</p><p>要检查每个注入文件的贡献大小（原始 vs 注入、截断，以及工具模式开销），使用 <code>/context list</code> 或 <code>/context detail</code>。参见<a href="/concepts/context.html">上下文</a>。</p><h2 id="时间处理" tabindex="-1">时间处理 <a class="header-anchor" href="#时间处理" aria-label="Permalink to &quot;时间处理&quot;">​</a></h2><p>当用户时区已知时，系统提示包括专用的<strong>当前日期和时间</strong>部分。为了保持提示缓存稳定，它现在只包括<strong>时区</strong>（无动态时钟或时间格式）。</p><p>当代理需要当前时间时使用 <code>session_status</code>；状态卡包括时间戳行。</p><p>配置：</p><ul><li><code>agents.defaults.userTimezone</code></li><li><code>agents.defaults.timeFormat</code>（<code>auto</code> | <code>12</code> | <code>24</code>）</li></ul><p>参见<a href="/date-time.html">日期和时间</a>了解完整行为详情。</p><h2 id="技能" tabindex="-1">技能 <a class="header-anchor" href="#技能" aria-label="Permalink to &quot;技能&quot;">​</a></h2><p>当存在符合条件的技能时，OpenClaw 注入一个紧凑的<strong>可用技能列表</strong>（<code>formatSkillsForPrompt</code>），包括每个技能的<strong>文件路径</strong>。提示指示模型使用 <code>read</code> 在列出的位置加载 SKILL.md（工作空间、托管或捆绑）。如果没有符合条件的技能，则省略技能部分。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;available_skills&gt;</span></span>\n<span class="line"><span>  &lt;skill&gt;</span></span>\n<span class="line"><span>    &lt;name&gt;...&lt;/name&gt;</span></span>\n<span class="line"><span>    &lt;description&gt;...&lt;/description&gt;</span></span>\n<span class="line"><span>    &lt;location&gt;...&lt;/location&gt;</span></span>\n<span class="line"><span>  &lt;/skill&gt;</span></span>\n<span class="line"><span>&lt;/available_skills&gt;</span></span></code></pre></div><p>这保持基础提示小，同时仍支持有针对性的技能使用。</p><h2 id="文档" tabindex="-1">文档 <a class="header-anchor" href="#文档" aria-label="Permalink to &quot;文档&quot;">​</a></h2><p>当可用时，系统提示包括一个<strong>文档</strong>部分，指向本地 OpenClaw 文档目录（repo 工作空间中的 <code>docs/</code> 或捆绑的 npm 包文档），还注明公共镜像、源代码仓库、社区 Discord 和 ClawHub（<a href="https://clawhub.com" target="_blank" rel="noreferrer">https://clawhub.com</a>）用于技能发现。提示指示模型首先查阅本地文档以了解 OpenClaw 行为、命令、配置或架构，并在可能时自行运行 <code>openclaw status</code>（仅在无法访问时询问用户）。</p>',33)])])}]]);export{s as __pageData,n as default};
